import { convertCelsiusToFarenheit } from "./convertCelsiusToFarenheit";
import { getImageUrl } from "./getImageUrl";
import { findMinMaxTemp } from "./findMinMaxTemp";
import { sortDataByDays } from "./sortDataByDays";

export {
  convertCelsiusToFarenheit,
  findMinMaxTemp,
  getImageUrl,
  sortDataByDays
}